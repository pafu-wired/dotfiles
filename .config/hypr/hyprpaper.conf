# Preload wallpapers (loads them into memory)
preload = ~/Pictures/simon-3.jpeg

# Set wallpaper for all monitors
wallpaper = ,~/Pictures/simon-3.jpeg

# Optional: Disable splash text
splash = false

# Optional: Enable IPC (for runtime control)
ipc = on
